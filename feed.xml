<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://mingyuk.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://mingyuk.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2025-07-31T04:30:17+00:00</updated><id>https://mingyuk.github.io/feed.xml</id><title type="html">Min-Gyu Kim’s Website</title><subtitle>Min-Gyu Kim&apos;s website, a container for my projects, thoughts, and a lot of other stuffs. </subtitle><entry><title type="html">Stable balancing weights - literature summary</title><link href="https://mingyuk.github.io/blog/2025/incomplete/" rel="alternate" type="text/html" title="Stable balancing weights - literature summary"/><published>2025-07-29T00:01:00+00:00</published><updated>2025-07-29T00:01:00+00:00</updated><id>https://mingyuk.github.io/blog/2025/incomplete</id><content type="html" xml:base="https://mingyuk.github.io/blog/2025/incomplete/"><![CDATA[<h1 id="summary">Summary</h1> <p>Weighting algorithms often suffer weight instability, i.e. high variance. Stable balancing weights (SBW) is a weighting method with minimal variance. It looks for weights that balances the distribution within a pre-specified threshold.</p> <h1 id="introduction">Introduction</h1> <p>Weighting algos have two goals:</p> <ul> <li>balance the empirical distribution of observed covariates (remove bias due to observed confounders)</li> <li>yield a stable estimate of the parameter of interest (minimize variance)</li> </ul> <p>IPTW doesn’t do both. Also, even with different methods that makes propensity score model less sensitive to model specification, the weights acquired is not intended to balance the distribution in the first place.</p> <p>SBW directly aims to optimize the weights so that it balances the covariate distribution, including marginal distributions.</p> <h1 id="the-estimation-problem">The Estimation Problem</h1> <ul> <li>$\mathcal{P}$: target population with $N$ elements</li> <li>$\mathcal{S}$: random sample from $\mathcal{P}$ of size $n$</li> <li>$\mathcal{R}$: sample of size $\mathcal{r}$ respondents from $\mathcal{S}$</li> <li>$Z_i$: response (treatment) indicator for unit $i$ in $\mathcal{S}$ ($Z_i = 1$ if unit $i$ responds)</li> <li>$Y_i$: outcome variable</li> <li>$X_{ip}$: $p$-th covariate of unit $X_i$</li> </ul> <p>The parameter of interest we want to estimate is the population mean: \(\overline{Y}_N = \frac{\sum_{i=1}^{N}{Y_i}}{N}\)</p> <p>Assume the response (treatment) is missing completely at random (MCAR) in $\mathcal{S}$, then \(\hat{Y}_r = \frac{\sum_{i=1}^{r}{Y_i}}{r}\) is an unbiased, consistent estimator of $\overline{Y}_N$.</p> <p>This is a strong assumption, and a more realistic assumption is non-respondents missing at random (MAR). Under this assumption, $Z_i$ and $Y_i$ are both related to observed covariates $X_i$ but not any unobserved covaraite $U_i$. (refer to “statistical analysis with missing data” by Little and Rubin)</p> <p>In this case, \(\hat{Y}_{\mathit{w}} = \frac{\sum_{i=1}^{r}{\mathit{w}_iY_i}}{r}\) is the unbiased, consistent estimator of $\overline{Y}_N$, given $\mathbf{w}$ appropriately adjusts for $X$. The most common way to calculate the weights is to fit a model that estimates the probability of responding (getting treatment) and inverting them. This is called the propensity score. An appealing aspect of using the propensity score is that it tends to balance the distribution of the observed covariates.</p> <p>However, this is a stochastic property (relies on the law of large numbers), and is not guaranteed even when the true mechanism of missing response is known. In reality the true mechanism is almost always unknown, which makes the balancing harder. Also, it is more desirable to balance features other than the covariate means, such as the marginal distribution. Finally, the weights can be unstable. The variance of $\hat{Y}_w$ is quadratic to $cv(\mathit{w}_i)$, the coefficient of variance. Even if a few units have weights close to zero, this will result in high variance of the estimate.</p> <h1 id="the-target-convex-optimization-problem">The Target Convex Optimization Problem</h1> <p>Rather than finding the weights for predicting response/treatment, SBW directly finds the weights with minimal variance that balances the covariates. This is the optimization problem that describes SBW:</p> \[\displaylines{ \min_{w}{\|w - \bar{w}\|_2^2}\\ \text{subject to } |\mathbf{w}^T\mathbf{X}_{\mathcal{R}_{p}} - \bar{X}_{\mathcal{S}_{p}}| \le \delta_p, p=1, ..., P\\ \mathbf{1}^T\mathbf{w} = 1,\\ \mathbf{w} \ge 0 }\] <p>$\mathbf{w}$ is a vector of size $r$, and $\bar{w}$ is the mean value vector of the weights. $\delta_{p}$ is a hyperparameter, defined by the user. This optimization effectively minimizes the coefficient of variance of the weights while balancing the covariates. Also, by manipulating $X_{\mathcal{R}}$, the constraint can balance other statistics such as variance. To do this, we augment $X_{\mathcal{R}}$ with additional covariate matrix \(\tilde{X}_{\mathcal{R}} = X_{\mathcal{R}}^2\).</p>]]></content><author><name></name></author><category term="machine-learning,"/><category term="causal-inference"/><category term="weighint algorithm"/><category term="machine learning"/><summary type="html"><![CDATA[Stable balancing weights is a weighting algorithm used in causal inference. Let's review the original literature.]]></summary></entry><entry><title type="html">SSH 터널링을 이용한 내부망 접근</title><link href="https://mingyuk.github.io/blog/2025/ssh-tunneling/" rel="alternate" type="text/html" title="SSH 터널링을 이용한 내부망 접근"/><published>2025-07-28T00:01:00+00:00</published><updated>2025-07-28T00:01:00+00:00</updated><id>https://mingyuk.github.io/blog/2025/ssh-tunneling</id><content type="html" xml:base="https://mingyuk.github.io/blog/2025/ssh-tunneling/"><![CDATA[<h1 id="왜-쓴-글인가">왜 쓴 글인가?</h1> <p>요즘 R studio server 등 연구실 내에서 돌리는 서비스를 외부에서 사용하기 위해 다들 다양한 방법을 쓰고 있는 것 같다. 그 중에서도 구글 원격 데스크톱처럼 화면을 전송하는 솔루션을 많이들 쓰는 것 같은데, 당연히 전송량이 많아 느리고 압축에 의해 화면이 깨지기도 하는 등 여러모로 많이 불편하다. 심지어 내부 정책에 의해 원격 데스크톱 프로그램이 막힐 때마다 옮겨다니는 유목민도 있는 것으로 보인다. 트래픽도 적고 빠르고 안정적인, 직접 브라우저로 서비스에 접근할 수 있는 방법이 있는데도 말이다. 혹시 본인이 이런 상황이라면 참고해보자. 작게는 NAS에 접근할 때도 사용할 수 있고, 주피터 서버 등등 다양한 상황에 활용할 수 있다.</p> <h1 id="ssh-터널">SSH 터널</h1> <p>일단 외부에서 SSH로 접속할 수 있는 호스트가 있긴 있어야 한다. 본인의 연구실 컴퓨터에 SSH server를 활성화하거나, 혹시 다같이 공유하는 jump host가 있으면 이를 통하는 것이 가장 편할듯. 기본적으로 내부 서비스와 통하는 트래픽을 이 ssh 호스트를 통하게 하는 방법이다. 보통 ssh로 접속하려면,</p> <div class="language-console highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="go">ssh user@host-ip
</span></code></pre></div></div> <p>와 같이 접속하는데, 이때 예를 들어 내부 주소 server-A, 포트 8080에서 서비스가 돌아간다고 하자. 그럼 원래 연구실 안에서 쓸 때는 브라우저에 server-A:8080이라고 쳤을 것이다. 외부에서 같은 방법으로는 당연히 내부 주소니까 접속이 불가능하다. 그래서 SSH 터널을 연결하여, localhost로 server-A의 내부주소를 대체하는 것이다.</p> <div class="language-console highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="go">ssh -L 1234:server-A:8080 user@host-ip
</span></code></pre></div></div> <p>이렇게 호스트에 접속하면, 호스트가 터널의 역할을 하게 된다. 접속한 쪽에서 localhost:1234로 오고 가는 트래픽이 실제로는 호스트를 통해 server-A:8080으로 전달되는 것이다. 그러면 SSH 연결을 한 뒤에 브라우저에서는 localhost:1234에 접속하면 원래 쓰던 서비스를 그대로 사용할 수 있다.</p> <h1 id="혹시-내부-주소를-사용하는-설정이-존재하는-경우">혹시 내부 주소를 사용하는 설정이 존재하는 경우</h1> <p>우리 연구실의 경우, 내부 주소를 hard-coded 방식으로 사용하는 설정이 있었다. 위의 가정을 그대로 들면 server-A의 포트 9090에서 돌아가는 다른 API를 8080에서 돌아가는 서비스가 server-A 내부 주소를 그대로 사용해서 호출하는 방식이었다. 그러니까 위의 방식처럼 8080 포트만 터널링을 하면, remote machine에서 그대로 그 내부 주소를 사용해서 API를 호출하므로 작동하지 않는다. 이 경우 두가지 작업이 필요하다.</p> <p>일단 server-A 내부 주소를 remote machine에서 localhost로 바꿔서 부르게 해야 한다. 도메인 주소가 설정된 경우, /etc/hosts 파일에 <code class="language-plaintext highlighter-rouge">127.0.0.1 domain-name</code> 한줄을 추가해두면 도메인 이름이 localhost로 resolve된다. 도메인 이름이 없이 실제 ip 주소만을 사용하는 경우, iptables를 직접 사용해야 한다. 이때 iptables 설정은 재부팅을 하면 초기화되므로 쉘 스크립트를 작성해두거나, iptables-persistent 등 다른 방법을 사용해야 한다. 나는 개인적으로 굳이 영구적으로 resolve할 필요도 없고 해서, 쉘 스크립트를 작성했다.</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/bin/bash</span>

<span class="c"># Define the target IP address and the redirect IP (localhost)</span>
<span class="nv">TARGET_IP</span><span class="o">=</span><span class="s2">"some-ip-address"</span>
<span class="nv">REDIRECT_IP</span><span class="o">=</span><span class="s2">"127.0.0.1"</span>

<span class="c"># Function to add the iptables rule</span>
add_rule<span class="o">()</span> <span class="o">{</span>
    <span class="nb">echo</span> <span class="s2">"Adding iptables rule: Redirecting traffic for </span><span class="nv">$TARGET_IP</span><span class="s2"> to </span><span class="nv">$REDIRECT_IP</span><span class="s2"> (localhost)..."</span>
    <span class="c"># Add the DNAT rule to the OUTPUT chain of the nat table</span>
    <span class="c"># This rule applies to packets originating from your local machine</span>
    <span class="nb">sudo </span>iptables <span class="nt">-t</span> nat <span class="nt">-A</span> OUTPUT <span class="nt">-d</span> <span class="s2">"</span><span class="nv">$TARGET_IP</span><span class="s2">"</span> <span class="nt">-j</span> DNAT <span class="nt">--to-destination</span> <span class="s2">"</span><span class="nv">$REDIRECT_IP</span><span class="s2">"</span>
    <span class="k">if</span> <span class="o">[</span> <span class="nv">$?</span> <span class="nt">-eq</span> 0 <span class="o">]</span><span class="p">;</span> <span class="k">then
        </span><span class="nb">echo</span> <span class="s2">"Rule added successfully."</span>
        <span class="nb">echo</span> <span class="s2">"You can verify with: sudo iptables -t nat -L OUTPUT -v -n"</span>
    <span class="k">else
        </span><span class="nb">echo</span> <span class="s2">"Failed to add the rule. Check your permissions and iptables setup."</span>
    <span class="k">fi</span>
<span class="o">}</span>

<span class="c"># Function to remove the iptables rule</span>
remove_rule<span class="o">()</span> <span class="o">{</span>
    <span class="nb">echo</span> <span class="s2">"Removing iptables rule: Redirecting traffic for </span><span class="nv">$TARGET_IP</span><span class="s2"> to </span><span class="nv">$REDIRECT_IP</span><span class="s2"> (localhost)..."</span>
    <span class="c"># Delete the DNAT rule from the OUTPUT chain of the nat table</span>
    <span class="nb">sudo </span>iptables <span class="nt">-t</span> nat <span class="nt">-D</span> OUTPUT <span class="nt">-d</span> <span class="s2">"</span><span class="nv">$TARGET_IP</span><span class="s2">"</span> <span class="nt">-j</span> DNAT <span class="nt">--to-destination</span> <span class="s2">"</span><span class="nv">$REDIRECT_IP</span><span class="s2">"</span>
    <span class="k">if</span> <span class="o">[</span> <span class="nv">$?</span> <span class="nt">-eq</span> 0 <span class="o">]</span><span class="p">;</span> <span class="k">then
        </span><span class="nb">echo</span> <span class="s2">"Rule removed successfully."</span>
    <span class="k">else
        </span><span class="nb">echo</span> <span class="s2">"Failed to remove the rule. It might not exist or there was an error."</span>
    <span class="k">fi</span>
<span class="o">}</span>

<span class="c"># Check if the script is run with sudo</span>
<span class="k">if</span> <span class="o">[</span> <span class="s2">"</span><span class="nv">$EUID</span><span class="s2">"</span> <span class="nt">-ne</span> 0 <span class="o">]</span><span class="p">;</span> <span class="k">then
    </span><span class="nb">echo</span> <span class="s2">"This script must be run with sudo. Please run: sudo ./</span><span class="si">$(</span><span class="nb">basename</span> <span class="s2">"</span><span class="nv">$0</span><span class="s2">"</span><span class="si">)</span><span class="s2"> [add|remove]"</span>
    <span class="nb">exit </span>1
<span class="k">fi</span>

<span class="c"># Main logic based on arguments</span>
<span class="k">case</span> <span class="s2">"</span><span class="nv">$1</span><span class="s2">"</span> <span class="k">in
    </span>add<span class="p">)</span>
        add_rule
        <span class="p">;;</span>
    remove<span class="p">)</span>
        remove_rule
        <span class="p">;;</span>
    <span class="k">*</span><span class="p">)</span>
        <span class="nb">echo</span> <span class="s2">"Usage: sudo ./</span><span class="si">$(</span><span class="nb">basename</span> <span class="s2">"</span><span class="nv">$0</span><span class="s2">"</span><span class="si">)</span><span class="s2"> [add|remove]"</span>
        <span class="nb">echo</span> <span class="s2">"  add    : Adds the iptables redirection rule."</span>
        <span class="nb">echo</span> <span class="s2">"  remove : Removes the iptables redirection rule."</span>
        <span class="nb">exit </span>1
        <span class="p">;;</span>
<span class="k">esac</span>
</code></pre></div></div> <p>chmod +x를 이용해 실행할 수 있는 스크립트로 만들고, ./script.sh add와 같이 실행하면 이제 현재 환경에서 server-A 주소로 오고가는 트래픽은 실제로는 localhost로 보내진다.</p> <p>이후 SSH 접속 시에 포트를 하나 더 터널링해야 한다.</p> <div class="language-console highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="go">ssh -L 1234:server-A:8080 -L 9090:server-A:9090 user@host
</span></code></pre></div></div> <p>이와 같이 하면 정상적으로 remote client에서 localhost:1234에 접속해서 원래 사용하던 서비스를 사용할 수 있다. (두번째 포트는 hard-coded여서 포트 숫자도 똑같이 맞춰줘야 했음)</p> <h1 id="맺음">맺음</h1> <p>별안간 원격 데스크톱으로 스스로의 혈압을 높이고 있는 원격 근무자들에게 도움이 되길. 웹 서비스 뿐만 아니라 내부망에서 사용되는 서비스들에 대부분 사용할 수 있다. SSH 터널링이라는 개념에 대해 잘 알아두자.</p>]]></content><author><name></name></author><category term="remote-work"/><category term="한국어"/><summary type="html"><![CDATA[SSH 터널링을 이용해 원격 데스크톱을 대체해보자.]]></summary></entry><entry><title type="html">Generative models</title><link href="https://mingyuk.github.io/blog/2024/generative-models/" rel="alternate" type="text/html" title="Generative models"/><published>2024-04-15T00:01:00+00:00</published><updated>2024-04-15T00:01:00+00:00</updated><id>https://mingyuk.github.io/blog/2024/generative-models</id><content type="html" xml:base="https://mingyuk.github.io/blog/2024/generative-models/"><![CDATA[<p>생성형 모델에 대한 관심이 높다. 생성형 모델이란 무엇인지, 어떻게 작동하는지, 수학적으로 어떤 원리를 바탕으로 하는지 살펴보자.</p> <figure class="align-center"> <img src="https://www.altexsoft.com/static/blog-post/2023/11/38a2d58b-265c-49d8-bf58-353da4f6c6ac.jpg" alt="생성형 모델의 샘플 생성" style="width: 100%; height: auto;"/> <figcaption>출처: https://www.altexsoft.com/static/blog-post/2023/11</figcaption> </figure> <p>두개의 그룹으로 묶인 샘플들이 있다고 가정하자. 이 분포를 따르는 새로운 샘플을 어떻게 생성할 수 있을까? 우선은 단순히 두개의 분포를 나타내는 확률밀도함수를 기반으로 새로운 시행을 하는 것을 생각해볼 수 있다. 이 확률밀도함수를 추정하는 것이 생성형 모델을 학습하는 과정의 핵심 과제다.</p> <p>간략하게 생성형 모델의 역사를 들여다보면서, 어떤 모델이 좋은 모델인지에 대해서도 알아보자.</p> <figure class="align-center"> <img src="https://cdn.neowin.com/news/images/uploaded/2021/05/1621148897_output.jpg" alt="Diffusion model과 GAN 모델의 비교" style="width: 100%; height: auto;"/> <figcaption>출처: https://cdn.neowin.com/news/images/uploaded/2021/05</figcaption> </figure> <p>위는 이미지 생성 모델의 결과이다. 어떤 결과가 더 좋은 결과일까? 여기서 보여주고자 하는 것은 이미지의 질이 아니다. 오른쪽으로 갈수록, 같은 대상에 대해 다양한 이미지가 생성된다는 것을 볼 수 있다. 왼쪽은 GAN, 오른쪽은 diffusion 모델이다. 더 다양한 이미지를 보여주는 diffusion model이 더 최신의 architecture이고, 생성형 모델에서는 하나의 class에 대해 다양한 이미지가 생성될 수 있는 것이 favorable하다.</p> <p>explicit, implicit model로 생성형 모델을 분류할 수도 있다. 위에서 언급한 트레이닝 샘플의 분포를 나타내는 확률밀도함수를 직접 수식으로 정의하고 근사하는 종류의 모델들이 explicit model이다. 수식을 명시적으로(explicitly) 정의하기 때문이다. 반면에 수식을 정확히 정의하지는 않으나, 어쨌든 학습 과정에서 확률밀도함수를 추정하게 되는 종류의 모델도 있다. 이를 implicit model이라고 하고, GAN이 이에 해당한다.</p> <p>생성형 모델을 평가하는 방법은 여러가지가 있을 수 있는데, 우선 생성된 이미지의 품질을 따질 수 있다. 그런데 품질을 어떻게 평가할 수 있을까? 좋은 품질의 이미지라면, 특정 클래스에 속할 확률이 명확하게 높아야 한다. 예를 들어 가방에 대한 이미지를 생성했는데 해당 이미지가 품질이 뛰어나다면 쉽게 가방으로 분류되어야 한다는 뜻이다. 그런데 가방 이미지를 모두 똑같이 생성한다면, 좋은 모델이 아니다. 따라서 클래스 하나에 속하는 이미지에 대해서는 넓은 분포를 가져야 한다. 또 다른 방법으로는 생성된 이미지의 분포와 실제 이미지의 분포 간 유사도를 비교해볼 수도 있다.</p> <p>이를 정량화하는 여러가지 metric이 있고, inception score나 Frechet inception distnace 등이 그 예시이다. Inception score는 앞서 말한대로 특정 클래스 내의 엔트로피는 높을수록, 특정 이미지의 클래스 라벨 엔트로피는 낮을수록 점수가 높다. Frechet inception distance는 생성된 이미지와 실제 이미지의 분포 간 거리를 따지는 방법이다.</p> <p>이제 GAN, diffusion model이 각각 어떻게 작동하는지, GAN부터 살펴보자. GAN은 Generative Adversarial Network의 약자다. 생성형 모델과 그 대항마(Adversarial) 네트워크가 서로 경쟁하면서 학습되는 구조를 가지고 있다. 따라서 명확한 확률분포함수를 정의하지 않는 implicit model이다. Generator network는 random noise를 input으로 받아 이미지를 생성하고, discriminator network는 이미지를 input으로 하여, 해당 이미지가 진짜 이미지인지 생성된 이미지인지 구분한다.</p> <figure class="align-center"> <img src="https://cdn.labellerr.com/GAN/All%20about%20GAN/vanillagan.webp" alt="GAN 모델의 아키텍처" style="width: 100%; height: auto;"/> <figcaption>출처: https://cdn.labellerr.com/GAN/All%20about%20GAN</figcaption> </figure> <p>따라서 훈련 과정에서는 generator / discriminator가 번갈아가며 훈련된다. 그런데 실제로는 이 훈련방법이 쉽지 않다. 첫번째 이유는 훈련 초반에는 생성된 이미지와 진짜 이미지를 구분하는 것이 매우 쉽기 때문이다. 따라서 discriminator network의 gradient가 0에 매우 가깝게 되고, gradient vanishing이 일어난다. 따라서 log와 기댓값을 취하는 방식으로 이런 문제를 최소화한다. 두번째 문제는 mode collapse라고 불린다. 간단히 말하면 generator가 discriminator를 속일 수 있는 hack을 찾아내는 상황이다. Discriminator network가 구분하지 못하는 이미지 하나를 포착해서, 그것만 생성하는 식으로 generator network가 손쉽게 이기는 상황이 생긴다. 그러면 더 이상 generator가 훈련되지 않고, 같은 이미지만을 계속해서 생성하게 되므로 훈련이 이루어지지 않는다.</p> <p>이러한 단점들이 있음에도, GAN을 여러가지 task에 활용해서 훈련시킨 결과물들이 있다. 말을 얼룩말로 바꾸는 모델, 얼굴 이미지를 생성하는 모델에서 출발하여, 텍스트로부터 이미지를 생성하는 모델까지 다양한 GAN 모델이 개발되었다. GAN의 기본 아이디어는 해상도 개선 등 다양한 도메인에 활용될 수 있기 때문이다.</p> <p>이제 diffusion model로 넘어가보자. 그러려면 우선 autoencoder부터 다시 시작해야 한다.</p> <figure class="align-center"> <img src="https://d1m75rqqgidzqn.cloudfront.net/wp-data/2020/04/29202749/Blog_info_29-04-2020-R-02-2048x1045.png" alt="Autoencoder" style="width: 100%; height: auto;"/> <figcaption>출처: https://d1m75rqqgidzqn.cloudfront.net/wp-data/2020/04/29202749</figcaption> </figure> <p>Autoencoder는 input data를 latent representation으로 나타내는 encoder, latent representation으로부터 원본 데이터를 복원시키는 decoder로 구성된다. 라벨 없이 데이터만 있어도 latent representation을 가능하게 하기 때문에 유용하며, 따라서 보통은 encoder만 사용하고 decoder는 버린다. 그런데, latent representation으로부터 원본 데이터를 생성할 수 있다면 decoder에 random noise를 투입하면 새로운 데이터를 생성할 수 있을까?</p> <p>결론은 그렇지 않다. Latent representation의 분포를 모르기 때문이다. Autoencoder를 훈련하는 과정에서 latent space에 대한 제한을 걸지 않으면 random noise가 의미 있는 데이터와 연관된다는 보장이 없다. 유의미한 데이터를 생성하려면 latent representation과 유사한 input을 decoder에 주어야 한다.</p> <p>이를 해결하기 위해 variational autoencoder(VAE)가 등장했다. VAE의 encoder는 probabilistic encoder라고 한다. 기존의 autoencoder는 같은 input이 주어지면 latent representation이 반드시 같은 deterministic model이다. 반면, VAE의 encoder는 확률론적인 개념을 도입해서 input이 주어졌을 때 latent representation을 평균과 분산을 이용해 표현한다. 이후 실제 representation은 이 확률 분포에서 sampling하여 가져온다. 수학적으로는 먼저 latent space의 분포(prior distribution)를 가정하고, input x가 주어졌을 때의 조건부 확률밀도함수의 평균과 분산을 encoder가 출력하는 것이다. 훈련할 때에는 원본 데이터의 likelihood를 최대화하는 방향으로 훈련한다. 또한 데이터 생성을 위해 만들어진 네트워크이므로 encoder를 버리고 decoder를 사용하게 된다.</p> <p>이제 마지막으로 diffusion model에 대해 알아보자.</p> <figure class="align-center"> <img src="https://insights.daffodilsw.com/hs-fs/hubfs/Allen/Diffusion%20Models%20moving%20to%20and%20from%20data%20and%20noise.png?width=3555&amp;height=2000&amp;name=Diffusion%20Models%20moving%20to%20and%20from%20data%20and%20noise.png" alt="Diffusion model" style="width: 100%; height: auto;"/> <figcaption>출처: https://insights.daffodilsw.com/hs-fs/hubfs/Allen</figcaption> </figure> <p>Diffusion model은 원본 이미지에 서서히 노이즈를 추가한 과정을 거꾸로 돌리는 법을 학습하는 모델이다. 임의의 이미지 X가 있을 때, Gaussian noise를 조금씩 추가하면 이미지 데이터셋의 분포는 Gaussian distribution이 된다. 이는 VAE에서 latent representation이 특정 분포를 따르게끔 유도되는 것과 유사하다.</p> <p>단계적으로 진행되는 프로세스를 Markov chain이라고 하며, 단계가 길수록 연산 시간이 오래 걸릴 수밖에 없다. 이때 forward process의 경우 직접 모든 단계를 계산할 필요 없이 특정 단계의 X를 즉시 계산할 수 있어 시간이 오래 걸리지 않는다.</p> <p>거꾸로 random noise로부터 원본 이미지까지 가는 과정을 생각해보자. Forward process를 어떻게 되돌릴 수 있을까? 핵심은 \(P(X\_{t-1} \mid X_t)\)을 학습하는 것에 있다. 현 상태에서 한 단계씩 이전 이미지를 추정하는 것이다.</p>]]></content><author><name></name></author><category term="machine-learning"/><category term="generative model"/><category term="machine learning"/><summary type="html"><![CDATA[Statistical understanding of generative models]]></summary></entry></feed>